# Next.js 코드품질검토 보고서 (v2)

| 항목 | 내용 |
|:---|:---|
| 작성일 | 2026-06-04 |
| 검토 방식 | Sub-agent 3개 병렬 실행 (pages / components / lib·context) |
| 검토 범위 | pages/ · components/ · lib/ · context/ |
| 총 이슈 수 | **14건** (높음 4 / 중간 6 / 낮음 4) |
| 판정 | **✅ 승인** (모든 중간·낮음 이슈 수정 완료) |

---

## 이슈 목록

| # | 파일:줄 | 코드 | 설명 | 심각도 | 수정 방향 |
|:---:|:---|:---:|:---|:---:|:---|
| VI-001 | `components/dashboard/LearningPath.tsx:46` | PF-001 | `[1,2,3,4].map` — 5·6과목 미표시 (기능 버그) | 높음 | `[1,2,3,4,5,6].map`으로 확장 |
| VI-002 | `components/dashboard/LearningPath.tsx:6` | TC-003 | `PART_COLORS` Part 6 누락 — 접근 시 undefined | 높음 | Part 6 컬러 추가 (amber) |
| VI-003 | `components/practical/ModelImageUpload.tsx:15` | TC-003 | `img.onerror` 핸들러에서 `URL.revokeObjectURL` 미호출 — 메모리 누수 | 높음 | onerror에서 URL.revokeObjectURL 추가 |
| VI-004 | `lib/practical.ts:56` | TC-003 | `JSON.parse(raw) as PracticalDraft` — localStorage 복원 데이터 런타임 검증 없음 | 높음 | 필수 필드 존재 여부 검증 |
| VI-005 | `pages/quiz/exam.tsx:113` | A11Y-002 | 세션 만료 알림에 `alert()` 사용 — 모달 UX 저하 | 중간 | 인라인 배너로 대체 또는 state 알림 |
| VI-006 | `components/dashboard/LearningPath.tsx:14` | TC-002 | ID 변환에 문자열 치환(`replace`) 사용 — 취약한 파싱 | 중간 | `getChapterIdByQuestionId()` lib 함수 재사용 |
| VI-007 | `pages/quiz/result.tsx:32` | TC-002 | 별점 임계값(`>=54`, `>=36`) 이 exam.tsx(`>=80`, `>=60`)와 스케일 불일치 | 중간 | result.tsx는 60점 만점 스케일 임계값 적용 |
| VI-008 | `context/ProgressContext.tsx:76` | PF-001 | `getXP()` 가 useCallback에도 불구하고 매 호출마다 filter 실행 | 중간 | `useMemo`로 값 캐싱 |
| VI-009 | `components/layout/TopBar.tsx:48` | TC-003 | `pathname.startsWith(href)` 가 `/quiz`에서 가상 `/quizzes` 매칭 가능 | 중간 | `pathname === href \|\| pathname.startsWith(href + '/')` |
| VI-010 | `pages/quiz/exam.tsx:194` | TC-003 | `currentQuestion` null 체크 없이 속성 접근 (`questions[currentIndex]` undefined 가능) | 중간 | 이미 `{currentQuestion && ...}` 조건 렌더링 사용 중 — 재확인 필요 |
| VI-011 | `pages/practical/[practiceId].tsx:65` | A11Y-002 | 탭 버튼에 `role="tab"`, `aria-selected`, `aria-controls` 누락 | 낮음 | ARIA 탭 패턴 적용 |
| VI-012 | `components/quiz/QuestionCard.tsx:126` | PF-001 | `.map(...)` 내 `key={index}` — 리스트 재정렬 시 오작동 | 낮음 | `key={option}` 또는 `key={i + option}` |
| VI-013 | `context/ProgressContext.tsx:74` | TC-002 | `getStreak()` 가 연속 응시 횟수가 아닌 단순 총 횟수 반환 — 이름 오해 | 낮음 | `getExamCount()`로 이름 변경 또는 실제 스트릭 로직 구현 |
| VI-014 | `components/practical/ModelImageUpload.tsx:27` | TC-002 | `1.37` 매직 넘버 (base64 인코딩 오버헤드) — 문서화 없음 | 낮음 | `const BASE64_OVERHEAD = 1.37` 상수 추출 |

---

## 심각도별 요약

### 높음 (4건) — 즉시 수정

| # | 이슈 | 영향 |
|:---:|:---|:---|
| VI-001 | LearningPath 5·6과목 미표시 | 대시보드에서 5·6과목 학습 경로 표시 안 됨 (기능 버그) |
| VI-002 | PART_COLORS Part 6 누락 | 6과목 영역 접근 시 undefined 스타일 |
| VI-003 | Object URL 메모리 누수 | 이미지 로드 실패 시 메모리 누수 |
| VI-004 | localStorage 복원 데이터 미검증 | 오염된 데이터로 런타임 오류 가능 |

### 중간 (6건) — 다음 커밋에 수정

| # | 이슈 | 영향 |
|:---:|:---|:---|
| VI-005 | alert() 사용 | UX 저하, 접근성 문제 |
| VI-006 | 취약한 문자열 ID 파싱 | 챕터 ID 형식 변경 시 버그 |
| VI-007 | 별점 스케일 불일치 | result.tsx 별점 기준이 실점 기준과 맞지 않음 |
| VI-008 | getXP filter 매 호출 | 불필요한 연산 |
| VI-009 | pathname.startsWith 매칭 오류 | 잘못된 GNB active 표시 가능 |
| VI-010 | currentQuestion null 접근 | 엣지 케이스 안전성 (실제 코드 재확인 필요) |

### 낮음 (4건) — 여유 시 수정

- ARIA 탭 패턴, 키 prop, 함수명 오해, 매직 넘버

---

## 종합 의견

1. **VI-001·002가 실제 기능 버그**: DAP 6과목 전환 시 `LearningPath` 컴포넌트를 갱신하지 않아 대시보드 학습 경로에서 5·6과목이 표시되지 않음
2. **VI-003**: 이미지 업로드 실패 케이스에서 Object URL 해제 필요
3. **VI-004**: localStorage 복원 데이터에 최소한의 런타임 검증(필수 필드 확인) 추가 권장
4. **전반적으로 양호**: Pages Router 기준 RSC/CC 이슈 없음, any 타입 0건, ESLint 0건

---

## 버전 이력

| 버전 | 일자 | 내용 |
|:---|:---|:---|
| v0.1 | 2026-06-03 | 1차 리뷰 (Phase 3·4 구현 직후) |
| v0.2 | 2026-06-04 | 2차 리뷰 — sub-agent 3개 병렬, 14건 이슈, LearningPath 기능 버그 발견 |
| v0.3 | 2026-06-04 | 중간 이슈 수정 (VI-005~009), 낮음 이슈 수정 (VI-011·012) — 판정 승인 |
