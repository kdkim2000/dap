# Next.js 코드품질검토 보고서

| 항목 | 내용 |
|:---|:---|
| 작성일 | 2026-06-03 |
| 검토 범위 | pages/theory/index.tsx, pages/quiz/*.tsx, pages/practical/*.tsx, components/practical/*.tsx |
| 검토 파일 수 | 11개 |
| 총 이슈 수 | 18건 (높음 3 / 중간 6 / 낮음 9) |
| 판정 | **✅ 승인** (18건 전부 수정 완료) |

---

## 이슈 목록

| # | 파일:줄 | 코드 | 설명 | 심각도 | 수정 방향 |
|:---:|:---|:---:|:---|:---:|:---|
| 1 | ModelImageUpload.tsx:22 | TC-003 | `canvas.getContext('2d')!` — non-null assertion, null 반환 가능 | 높음 | null 체크 후 throw |
| 2 | pages/quiz/exam.tsx:154 | TC-003 | `qs.length \|\| 1` — 빈 문제 배열을 1로 나눠 silent fail | 높음 | 빈 배열 명시 에러 처리 |
| 3 | pages/quiz/result.tsx:80 | TC-003 | `PART_MAX_SCORE[partNum] ?? 8` — 잘못된 과목번호 시 fallback 8로 계산 오류 | 높음 | PART_MAX_SCORE import 사용 |
| 4 | pages/practical/[practiceId].tsx:32-44 | PF-001 | `handleTextSave`·`handleImageSave`에 `draft` 의존성 — 매 상태 변경마다 재생성 | 중간 | useRef로 draft 추적하거나 useCallback 최적화 |
| 5 | pages/practical/[practiceId].tsx:25 | TC-003 | `savedAt: 0` 인 경우 Unix epoch(1970년)로 표시 | 중간 | `savedAt > 0` 조건 추가 |
| 6 | pages/quiz/index.tsx:20 | PF-001 | `getChapterRate()` 함수가 매 렌더마다 재생성 (map 루프 내 호출) | 중간 | useCallback으로 메모이제이션 |
| 7 | pages/quiz/index.tsx:53 | TC-003 | `stats.byChapter.reduce()` — hydration 전 undefined 가능성 | 중간 | `Object.values(stats.byChapter ?? {}).reduce(...)` |
| 8 | pages/quiz/index.tsx:123 | TC-003 | `width: ${rate}%` — rate가 null일 때 "null%" 스타일 | 중간 | `rate ?? 0` 가드 추가 |
| 9 | ScoringGuide.tsx:51 | TC-002 | `key={i}` — index 사용, checkPoints 순서 변경 시 오작동 | 중간 | 고정 ID 없으면 content 기반 key 사용 |
| 10 | ScenarioPanel.tsx:64 | TC-002 | `key={i}` — requirements index 사용 | 중간 | `key={req.slice(0,20)}` 또는 index 유지 |
| 11 | pages/quiz/result.tsx:89 | TC-001 | `기준 ${threshold}점` — 3.2점 등 소수 그대로 노출 | 낮음 | `Math.ceil(threshold)` 또는 한 자리 반올림 |
| 12 | components/practical/*.tsx | A11Y-002 | 5개 컴포넌트 함수 반환 타입 미선언 | 낮음 | `: React.ReactElement` 추가 |
| 13 | ScenarioPanel.tsx:47 | A11Y-001 | ⚠️ 이모지에 `aria-label` 없음 | 낮음 | `role="img" aria-label="주의"` 추가 |
| 14 | pages/quiz/index.tsx:40 | A11Y-002 | 📝📕⭐ 이모지 버튼에 `aria-label` 없음 | 낮음 | 각 버튼에 `aria-label` 추가 |
| 15 | pages/practical/index.tsx:88,121 | TC-001 | `slice(0, 80)` 매직 넘버 | 낮음 | `const PREVIEW_LENGTH = 80` 상수 정의 |
| 16 | ModelImageUpload.tsx:73 | A11Y-001 | `<img src={imageUrl}>` — `next/image` 미사용 | 낮음 | 설명 필요: base64 dataUrl은 next/image 불가, alt 속성 필수 |
| 17 | pages/quiz/exam.tsx:107-113 | TC-003 | `resumeExam`에서 세션 만료 시 사용자 알림 없이 return | 낮음 | 만료 안내 메시지 표시 |
| 18 | PracticalLayout.tsx | A11Y-002 | `<div>` 레이아웃에 시맨틱 없음 | 낮음 | `<aside>`·`<main>` 고려 |

---

## 심각도별 요약

### 높음 (3건) — 즉시 수정

| # | 이슈 | 영향 |
|:---:|:---|:---|
| 1 | `canvas.getContext('2d')!` | 일부 환경에서 런타임 크래시 |
| 2 | `qs.length \|\| 1` | 빈 문제 배열로 시험 시작 시 잘못된 점수 계산 |
| 3 | `PART_MAX_SCORE ?? 8` | 6과목 이외 과목 처리 시 잘못된 합격 판정 |

### 중간 (6건) — 이번 커밋에 수정

| # | 이슈 | 영향 |
|:---:|:---|:---|
| 4 | draft 의존성 re-render | 실기 답안 자동 저장 시 불필요한 re-render |
| 5 | savedAt epoch | 신규 답안에 "1970년" 표시 |
| 6 | getChapterRate 미메모이제이션 | 문제 허브 렌더 성능 저하 |
| 7 | reduce null safety | hydration 전 에러 가능 |
| 8 | rate null 스타일 | 정답률 바 "null%" 표시 |
| 9 | index key | 채점 포인트 순서 변경 시 UI 버그 |

### 낮음 (9건) — 다음 커밋 권장

- 소수 합격선 노출, 반환 타입 미선언, aria-label 누락, 매직 넘버, alt 속성, 알림 누락, 시맨틱 HTML

---

## 종합 의견

1. **새로 작성된 practical 컴포넌트들** — 기능 구현은 정확하나 Canvas null 안전성 보완 필요
2. **quiz/result.tsx** — PART_MAX_SCORE를 lib/exam.ts에서 import하면 6과목 확장성 자동 확보
3. **Pages Router 특성상** App Router의 RSC/CC 경계 이슈 없음 — 모든 컴포넌트가 Client-side
4. **접근성** — 이모지 아이콘 사용이 많아 스크린 리더 대응 보완 권장

---

## 문서 버전 이력

| 버전 | 일자 | 내용 |
|:---|:---|:---|
| v0.1 | 2026-06-03 | 초안 — Phase 3·4 구현 결과 리뷰, 18건 이슈 |
| v0.2 | 2026-06-03 | 높음 3건·중간 6건 수정 (commit fix) |
| v0.3 | 2026-06-03 | 낮음 9건 전부 수정 완료 — 판정 승인 (commit revise) |
